---
# tasks file for juliovp01.rhel-kvm-install
# tasks files on main.yml as include arguments
- include: setup.yml
# - include: registration.yml

- set_fact:
    ssh_pubkey: "{{ ssh_cmd.stdout }}"

- set_fact:
    vm_name: "rhel-7-{{ 10000 | random }}"
    cacheable: true
- include: install.yml
  # - include: template.yml
- include: create_vm.yml
